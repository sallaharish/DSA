// /create the function declearatoio
// use the if else conditon inorde rto check the age of the person and then retuent he string
// for checking the age of the person 
function checkage(age) {
    if (age > 18) {
        return ("egligible for vote")
    }
    return "not agligible"

}


console.log(checkage(12))

// for checking the number is even or odd
function evenorodd(num) {
    if (num % 2 == 0) {
        return "even"
    }
    return "odd"
}

console.log(evenorodd(1))